<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <PackageId>AiSdk.UiStreamProtocol</PackageId>
    <Description>Core types, SSE reader/writer, and message assembler for the Vercel AI SDK UI Stream Protocol.</Description>
  </PropertyGroup>

  <ItemGroup>
    <Compile Remove="AiSdk.UiStreamProtocol.AspNetCore\**\*" />
  </ItemGroup>

</Project>
